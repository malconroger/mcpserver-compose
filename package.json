{
  "name": "mcp-server",
  "version": "1.0.0",
  "scripts": {
    "start": "mcp-server --port 4444 --tools ./tools"
  },
  "dependencies": {
    "@modelcontextprotocol/server": "https://github.com/modelcontextprotocol/server.git"
  }
}
